# ====  ANSIBLE   =============================================================
#          NAME:  roles/install-dotfiles/handlers/post-cleanup.yml
#   DESCRIPTION:  Main entry point for common tasks
# =============================================================================
---
- name: Post-installation cleanup
  ansible.builtin.file:
    path: "{{ role_tempdir }}"
    state: absent
  listen: post-install-cleanup
