# ====  ANSIBLE   =============================================================
#          NAME:  group_vars/all/main.yml
#   DESCRIPTION:  Common variables that apply to this play
# =============================================================================
---
- name: Save SSH private key file
  ansible.builtin.copy:
    content: "{{ install_dotfiles_ssh_key_output.value }}"
    dest: "{{ user_account.home_dir }}/.ssh/id_{{ user_account.ssh.key_type }}"
    owner: "{{ ansible_user_id }}"
    group: "{{ ansible_user_gid }}"
    mode: "0600"
  listen: save-ssh-key
